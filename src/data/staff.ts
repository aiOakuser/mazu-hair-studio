import type { StaffMember } from "@/lib/types";

// Staff roster isn't in the reference template — ships empty until real
// stylist data is available (Staff tab shows a "coming soon" state).
export const staff: StaffMember[] = [];
